[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
filterwarnings = ignore::DeprecationWarning
# Ensure the backend directory is on the Python path so `from app.main import app` works
pythonpath = .
addopts = -v --tb=short
